package org.teamhub.groupware.user.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.teamhub.groupware.user.entity.OverseasPresentation;

public interface OverseasPresentationRepository extends JpaRepository<OverseasPresentation, Long> {
}
